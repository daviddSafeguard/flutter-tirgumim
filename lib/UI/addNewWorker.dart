import 'package:fluro/fluro.dart';
import 'package:flutter/material.dart';

class AddNewWorker extends StatelessWidget {
  const AddNewWorker({Key key}) : super(key: key);

  //void _navigateToSecondPage(BuildContext context) async {
  //  if (FormBuilder.of(context).saveAndValidate()) {
  //    await Navigation.router.navigateTo(context, Navigation.secondFormPage,
  //        transition: TransitionType.material);
  //  }
  //}

  Widget build(BuildContext context) {
    return Scaffold();
  }
}
